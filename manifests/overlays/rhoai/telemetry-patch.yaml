# Patch to enable telemetry for RHOAI deployments
apiVersion: apps/v1
kind: Deployment
metadata:
  name: training-operator
  namespace: kubeflow
spec:
  template:
    spec:
      containers:
      - name: training-operator
        env:
        - name: TELEMETRY_ENABLED
          value: "true"
